# Floors only — the reproducible pin is constraints.txt (pip freeze of the
# authoritative venv; see docs/PLAN-pydantic-ai-v2.md Phase U0). Install with:
#   pip install -r requirements.txt -c constraints.txt
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.0
python-multipart>=0.0.9
pydantic-ai>=1.77.0
openpyxl>=3.1.0
# RUN-REVIEW P0-3 (2026-04-26): pure-Python Excel formula evaluator
# used by tools/recalc.py to write cached values into merged workbooks
# so downstream programmatic readers (compare_results.py, the agent's
# verify_totals when it reads sub-sheet rollups) don't see None.
formulas>=1.2
PyMuPDF>=1.24.0
openai>=1.0.0
litellm[proxy]
beautifulsoup4>=4.12.0
# Windows corporate proxies (e.g. PwC genai-sharedservice-emea) present a
# MITM cert chain rooted at an internal CA that lives in the Windows cert
# store, not in certifi. truststore.inject_into_ssl() (called at startup
# in server.py) makes Python's ssl module read the OS cert store so httpx
# / openai / anthropic clients trust the corporate root automatically.
# No-op on Mac (LiteLLM proxy is plain HTTP localhost). Requires Py 3.10+.
truststore>=0.9.0
# Auth layer (PLAN-azure-auth-deployment Phase 1): argon2id password hashing.
# argon2id is the modern memory-hard KDF — never store a fast/plain hash for
# the email+password login. Cookie signing uses stdlib hmac (no extra dep).
argon2-cffi>=23.1.0
# Word (.docx) input (PLAN-word-input.md). mammoth is a small pure-Python
# .docx→HTML extractor for the notes source-formatting side-channel
# (deliberately NOT the removed docling/torch stack). docx2pdf drives the
# installed Microsoft Word to convert .docx→PDF on the Windows box; it is
# Windows-only (marker below) — on Mac/Linux/cloud the converter is LibreOffice
# (`soffice`), a system package, not a pip dependency.
mammoth>=1.6.0
docx2pdf>=0.1.8 ; sys_platform == "win32"
# Test tooling: pytest-xdist parallelises the backend suite across cores.
# The full suite is ~3100 tests and runs ~4x faster with `pytest tests/ -n auto`
# (serial ~240s → ~60s). Deliberately NOT wired into addopts — worker spawn +
# per-worker imports make a single-file/TDD run SLOWER, so parallelism is an
# explicit `-n auto` flag for the full suite only (see CLAUDE.md → Testing).
pytest-xdist>=3.8.0
