"""Canonical concept-model package (docs/PRD-canonical-concept-model.html).

Phase 0 — read-only template parsing.  Later phases will add the DB
importer, facts API, cascade recompute, and Excel exporter.
"""
