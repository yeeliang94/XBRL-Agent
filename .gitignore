# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
*.egg-info/

# Node
node_modules/
dist/
*.tsbuildinfo
.vite/

# Environment
.env
.env.local

# Output (regenerated per run)
output/

# SQLite databases (may contain extracted financial figures + run history)
*.db
*.db-wal
*.db-shm

# Data (large files, may be confidential)
data/

# Logs
*.log

# IDE / OS
.DS_Store
.vscode/
.idea/

# Excel lock files
.~lock.*#
XBRL-Agent/

# Phase 4.2 A/B harness scratch (live-run DBs, rendered xlsx, copied PDFs)
ab_work/

# Docling model weights for the scanned-PDF→readable-doc feature (~599MB).
# Fetched once via scripts/fetch_docling_models.py; bundled into the deploy
# artifact, never committed (too large for git). See docs/PLAN-scanned-pdf-to-doc.md.
models/
